[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

[tool:pytest]
markers =
    unit: Unit tests
    integration: Integration tests
    etl: ETL tests
    api: API tests
